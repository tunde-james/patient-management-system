spring:
  application:
    name: patient-service
  output:
    ansi:
      enabled: ALWAYS
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 4000

logging:
  level:
    root: INFO

grpc:
  server:
    port: -1

billing:
  service:
    address: localhost
    grpc-port: 9001