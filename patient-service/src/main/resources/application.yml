spring:
  application:
    name: patient-service
  output:
    ansi:
      enabled: ALWAYS

server:
  port: 4000

logging:
  level:
    root: INFO

billing:
  service:
    address: localhost
    grpc-port: 9001